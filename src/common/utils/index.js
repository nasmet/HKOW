import * as other from './other';
import * as time from './time';
import * as type from './type';
import * as number from './number';
import * as regex from './regex';

export default Object.assign(other, time, type, number, regex);